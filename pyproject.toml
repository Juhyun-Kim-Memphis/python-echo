[project]
name = "python-echo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.9.0",
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
